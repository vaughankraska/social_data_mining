[project]
name = "network-analysis"
version = "0.1.0"
description = "Lib for social network analysis"
readme = "README.md"
authors = [
    { name = "vaughankraska", email = "vaughankraska@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "igraph>=0.11.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
